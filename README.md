# MPP-Pipeline
This application takes .MPP files and  converts them automatically to .XML for easier parsing and drops them into their own folder of your choice.
It will ignore any non .MPP file and you can set rules for what  files to export (rules will depend on the fileName)
